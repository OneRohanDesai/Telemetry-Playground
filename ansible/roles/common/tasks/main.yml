---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install packages
  apt:
    name:
      - curl
      - git
      - unzip
      - ca-certificates
      - gnupg
      - software-properties-common
    state: present
