apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-dashboard-infrastructure

  labels:
    grafana_dashboard: "1"

data:
  telemetry-overview.json: |
{{ .Files.Get "observability/grafana/dashboards/infrastructure.json" | indent 4 }}
