apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-dashboard-overview

  labels:
    grafana_dashboard: "1"

data:
  telemetry-overview.json: |
{{ .Files.Get "observability/grafana/dashboards/telemetry-overview.json" | indent 4 }}
