apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-dashboard-receivers

  labels:
    grafana_dashboard: "1"

data:
  telemetry-overview.json: |
{{ .Files.Get "observability/grafana/dashboards/receivers.json" | indent 4 }}
